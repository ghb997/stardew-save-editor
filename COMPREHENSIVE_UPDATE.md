# 0.6.0（build 14）功能完善

本轮以 build 13 的加载优化和 build 12 的 iPad 适配为基线，增加四个常用工具。地图、外观、房间、背包和其他已有功能继续保留。

## 参考依据

用户指定的 StardewEditor.ipa 是 Stardew Editor 3.0.3（103），SHA-256 为 `f872da346f86eaf853d1017d4dfa39606cc4f983a3359ef2a2ba08ded72961d4`。本地只读检查的 Info.plist 路径为 `Payload/Runner.app/Info.plist`；`Payload/Runner.app/Frameworks/App.framework/App` 含 `Open Chest/Fridge`、`Finish Machines`、`Weather & Luck`、`Tomorrow's Weather`、`bundles_item_add`、`BundleRequirement` 等功能字串。

字串只能证明对应功能线索，不能证明完整界面和运行逻辑。本项目根据现有 XML 保存架构独立实现，未运行、解密或复制参考程序的代码与资源。

| 工具 | 新增功能 | 写入规则 |
| --- | --- | --- |
| 箱子与冰箱 | 按地点、坐标、物品搜索；查看空位；添加、替换、清空普通物品；数量、品质编辑；恢复容器 | 识别现有普通/石箱、36 格冰箱及 70 格大箱；保留未改槽位、颜色、modData 和未知节点 |
| 天气与运气 | 山谷和姜岛明日天气，原值对照，运气数值和预设，按项恢复 | 仅改已有有效字段；同步山谷根字段与 Default 上下文；保留整数/名称编码；每日运气 -0.1 至 0.1 |
| 机器加工 | 设备/地点/产物/坐标搜索，加工中和待保存筛选，选择、预览完成、单台撤销 | 对 14 种已识别机器将剩余计时归零并设为可领取，保持产物身份、数量、品质及额外字段 |
| 社区中心 | 区域与状态筛选，收集包和材料搜索，普通/混合收集包进度，材料选择和背包补给预览 | 读取存档的 bundleData 与 bundles，严格配对材料与布尔进度；补给只占空格，不直接改交付、奖励或路线标记 |

## 操作方式

在“工具”内打开对应模块，所有修改先进入草稿。“检查与保存”会按模块显示变化，并支持逐项撤销。容器编辑弹窗的“取消”放弃该弹窗的变化；容器内“恢复此容器的全部修改”不影响其他模块。

社区中心需逐项选择材料。任选收集包会显示还需交付的数量；每项补给完整需求数量，不扣除当前背包已有同类物品。预览明确显示材料、品质、数量和目标格子，空位不足时整次操作取消。补给后献祭进度保持不变，保存并回游戏交付材料。

## 边界

- 共享 Junimo 箱、未知特殊箱、扩展容量、工具、装备、任务物品和不明物品保持只读；不创建容器、不移动建筑或家具。
- 仅操作已存在的世界地点，包括建筑 indoors 内的容器；不混入玩家背包、家具内部或联机玩家的专属物品。
- 重复坐标、重复天气、未知天气、冲突上下文不会自动修复。节日、婚礼、绿雨等特殊天气不由此工具生成；游戏特殊日规则仍可能覆盖明日天气。
- 不改变当前天气的雨雪布尔状态。每日运气在游戏进入新一天后可能重新生成。
- 机器限定小桶、熔炉、罐头瓶、压酪机、织布机、产油机、回收机、宝石复制机、蛋黄酱机、煤炭窑、咖啡机、脱水机、鱼类烟熏机和重型熔炉；不涉及陈酿桶品质、孵化器成长或空机器。
- 收集包定义或进度缺失、重复、长度不一致时，不假定“未完成”。Joja 会员仅查看旧献祭信息，金币需求在游戏中支付，未知材料不自动补给。
- 四个模块全部沿用现有后台解析、来源哈希、预保存备份、双文件事务和失败回滚；本轮不更改这些事务协议。

## 验证

新增 23 项存档回归覆盖容器解析与写入、只读边界、天气同步、机器产物保留、献祭材料补给原子性、差异撤销、zlib 与后台再解析。新增四条原生 UI 流程检查弹窗、预览、草稿与检查入口。2026-09-12 完成设备 Release 构建；iPhone SE 上完整 138 项测试全部通过，iPad mini 与 13 英寸 iPad Pro 各 4 条新增流程通过，合计 146 次测试执行通过。24 张新增流程截图已逐张审阅。详细结果与真实游戏验证限制见 [构建 14 验证记录](validation/BUILD14_VERIFICATION.md)。
