# v0.3.3 · build 9 验证候选（2026-09-09）

- 参考 Stardew Guide & Tracker 的农场概览、分类胶囊与收藏分组层次，改造原生追踪首页。
- 保留全部 11 个模块，按基础、收藏、农场生活、存档状态分组；支持筛选、折叠和详情内横向切换。
- 加入农场摘要、日期、四类收藏合计、配方解锁和未保存草稿提示。
- 收藏详情采用分组卡片；技能与钱包使用真实指标进度；配方可筛选全部、已解锁、未解锁并搜索。
- 追踪页单独配置浅色/深色语义颜色，大字号摘要与数据行支持纵向重排；不更改编辑器的保存流程和底栏原生图标策略。
- 警告优先于草稿状态，收藏合计不称为完美度，持有核桃不称为累计发现，比例夹到有效范围。
- 拆分首页、组件和详情文件，新增指标 helper 与 7 个 XCTest，以及仅 Debug 可用的追踪详情截图入口。
- 静态检查通过；原生编译、测试执行和视觉验收尚未完成。版本号提高到 0.3.3（9）以区分旧版；云端验证进行中，尚未发布 Release。

---

# v0.3.3 · build 8（历史发布）

交付日期：2026-09-08。修复 build 7 在底部标签栏使用大尺寸游戏贴图造成的左下角溢出。

## 界面修复

- 主页标签原先通过自定义 `GameLabel` 提供 160×144 农舍图。系统 `UITabBar` 会读取图片固有尺寸并忽略自定义 SwiftUI frame，导致农舍从左下角放大并遮挡追踪、工具和设置入口。
- 四个底部标签改为系统管理尺寸的 `Label` 与导航符号，保持统一的选中颜色、安全区和可访问性；页面题头、工具入口、编辑板块、字段行与实体预览继续显示游戏素材。
- 构建号提高到 8，便于覆盖安装 build 7。
- [GitHub Actions run 34192090963](https://github.com/ghb997/stardew-save-editor/actions/runs/34192090963) 已使用 Xcode 16.4 与 iOS SDK 18.5 成功生成 Release、arm64、iPhoneOS 未签名 IPA；SHA-256 为 `3cc1b58e4a94905b2004ae163b1afd5035fc662215b5b684c1026d83fdd1a441`，签名后方可安装。

---

# v0.3.3 · build 7

交付日期：2026-09-08。基于 v0.3.2 build 6 增补用户提供的《星露谷物语》素材库。

## 图像完善

- 安全审计 `星露谷素材800+.zip` 的 859 张图片：803 PNG、27 GIF、29 JPEG；逐项检查压缩路径、图片解码、尺寸、动画帧与 SHA-256，没有执行包内内容。
- 从中筛选接入 58 张图片：11 种农场动物、8 种成熟果树、冈瑟/州长/莫里斯/蜗牛教授/雷欧/马龙/齐先生 7 位补充人物、鱼塘建筑，以及 31 张用于界面识别的原版素材。
- 工具页与追踪页不再给多个板块重复套用同一张背景图；角色、外观、房屋、背包、进度、关系、技能、钱包、动物、配方、检查保存、备份、作物计算器和魔法地图使用各自图像。进入编辑板块后，固定顶栏继续显示当前板块图像和名称。
- 五项技能分别使用锄头、鱼竿、斧头、镐子和银河剑；财富、矿物、金色核桃、古物、鱼类与怪物统计显示对应素材；农场批量操作使用喷壶、石块、纤维和木材图，方便辨认操作对象。
- 动物编辑页和地图现在显示白牛、棕牛、山羊、猪、绵羊、兔子、鸭、鸵鸟，以及白/棕/蓝鸡物种示意。三种鸡使用幼年游戏图，界面明确说明不表示实际年龄或动作。
- 地图根据 `treeId`/旧版 `indexOfFruit` 同时识别树苗 ID 与果实 ID，显示樱桃、杏、橙子、桃子、石榴、苹果、芒果、香蕉树；图片表示成熟物种，不冒充当前生长阶段。
- 地图新增鱼塘图像；关系页补齐 7 位此前缺少的人物头像。新增 128×128 单帧头像完整显示，既有多表情图集仍裁取第一张 64×64 表情，避免把整套表情缩成一个头像。
- 虚空鸡、金鸡、恐龙、肤色色板和无法从存档可靠判断的素材仍显示无预览。入口及字段提示图只表达正在查看或修改的功能，不冒充存档中的实际解锁状态。
- 打包位图由 55 张增至 113 张。新增文件在 `GAME_ASSETS_MANIFEST.json` 中记录原压缩包名称、压缩包 SHA-256、包内路径、文件 SHA-256、尺寸和使用说明。

## 版本与构建

- 版本更新到 0.3.3 build 7。[GitHub Actions run 34190220247](https://github.com/ghb997/stardew-save-editor/actions/runs/34190220247) 已使用 Xcode 16.4 与 iOS SDK 18.5 成功生成 Release、arm64、iPhoneOS 未签名 IPA；SHA-256 为 `96621789da9f3a1e6bf7030851cef22ad34b60b98c4f996bb0008ffaf2ef5d4c`，签名后方可安装。

---

# v0.3.2 · build 6

交付日期：2026-09-07。基于 v0.3.1 build 5 的独立副本修改，未改动原始 ZIP、原版源码或用户游戏存档。

## 存档安全与正确性

- 杂物识别与删除共用检查逻辑，保留 XML 物品类型与命名空间，排除大型制造物、容器、家具、持有物和未知模组类型。普通杂草与同裸 ID 的盆栽不再混淆。
- 保存中断日志绑定标准化文件来源。同名不同目录、复制导入的副本互相隔离；同一对文件切换目录授权与双文件授权仍使用同一来源身份。
- 自动恢复只处理可解释的原始/目标混合状态。遇到无法识别的新进度，先保存保护备份并提示冲突；旧的无来源日志不再自动覆盖当前文件。
- 双文件检查、备份、写入、校验与回滚置于一次文件协调范围。备份写入后重新读取校验；待恢复和失败事务的备份不受普通保留策略清理。
- 备份导出独立于当前编辑会话，原档损坏或缺失时仍可从有效备份导出文件。手动关键节点备份设为保护备份，普通成功保存的备份默认保留最近 10 份。
- 修改动物时按 ID 和实际变化字段逐项写回；修改饲养天数不再同步覆盖年龄，其他动物和未编辑字段保留原值。
- 全部差异自动分组，增加未知分组兜底，财富、矿洞、钱包能力和动物修改均能展示；逐项撤销与地图坐标差异同步。

## 兼容性与后台处理

- XML 格式探测读取 BOM 和首个有效字节，避免固定 256 字节截断中文或 emoji 造成误判。
- SaveGameInfo 必须使用 Farmer 根节点；双方具有非零稳定玩家 ID 时核对配对，缺少 ID 时保留兼容性提示。
- 等级、经验与职业采用共享更新规则；跨越 5/10 级门槛时统一清理失效职业。未编辑的未知或扩展组合保持原状。
- 旧版钱包布尔字段区分 true/1、false、缺失与 nil；只更新用户修改的能力，不顺带迁移其他标记。
- 独立 actor 执行文件工作与 XML 解析，Swift 6 `sending` 转移独占结果，后台不共享 UI 的可变 XML 树。工作期间冻结草稿，完成后再解锁与回调。
- 物品、配方、作物、备份和文件服务独立初始化；目录异常不会直接阻断其他服务。备份列表刷新失败单独显示，不把已成功的写回报告为失败。
- 新增胡萝卜、西葫芦、西兰花、霜瓜及其种子八种已确认的字符串 ID，安全添加目录由 590 项增至 598 项。

## 编辑体验

- 编辑模块和地图底部固定显示检查入口；检查页支持逐项恢复原值。
- 复制导入使用明确的“副本已保存 → 导出 → 核对目标”流程；保存完成回调驱动导出，不再依赖固定延时。导出取消与完成分别提示。
- 备份管理展示来源、原因、保护与校验信息，支持手动备份、校验、导出和匹配农场恢复。
- 物品库完整浏览、分类、收藏与最近添加；数量可直接输入或使用常用值。
- 地图列出全部影响对象及坐标，并去重批量与单项操作；提供可供 VoiceOver 操作的标准对象列表。
- 新农场读取成功后才替换当前会话，取消或读取失败保留原上下文。
- 按功能拆分主页、追踪、工具、设置及各编辑模块；统一反馈组件协调弹层中的错误、成功和进度显示。

## 游戏图像

- 新增 50 套来源固定的游戏素材，总计打包 55 张位图（包含既有图像与应用图标）。33 位 NPC 肖像、工具、制作物、发型、饰品、基础建筑与墙纸地板使用原始游戏纹理。
- 移除自绘墙纸配色样式、动物 emoji 和无法对应真实地形的地图装饰。纹理按像素裁切并使用最近邻显示。
- 没有取得可靠原始贴图的动物、肤色及未知或模组对象显示无预览，不生成替代图片。完整人物合成、作物生长阶段外观、升级建筑外观尚未补齐。
- 素材固定提交、路径、SHA-256 与尺寸见 GAME_ASSETS_MANIFEST.json；游戏素材来源及版权说明见 GAME_ASSET_PROVENANCE.md。

## 验证与后续

新增并整理为 51 个 XCTest 测试方法。本次 Windows 环境仅完成静态检查，未编译或运行这些测试；详见 VALIDATION.md。交付包含 Mac 模拟器构建测试脚本。

此前评审中“其后可以考虑”的草稿跨重启恢复、可配置材料预设、多人玩家切换、社区中心/剧情/建筑编辑仍保留为后续路线图。本轮先落地已识别的可靠性修复、已有能力补全与素材替换，未扩展这些复杂写入领域。
