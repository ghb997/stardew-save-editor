# v0.4.0 build 10 · Editor improvements

本轮依据参考应用的功能线索补齐常用编辑流程，沿用现有四栏导航和统一保存入口。参考包仅作本地只读对照；应用内字串不能证明完整运行行为，本项目按已有存档结构独立实现。

## 功能对照

| 功能 | 原有状态 | 本次实现 |
| --- | --- | --- |
| 背包容量 | 仅显示已序列化的槽位 | 12/24/36 格容量选择、缩容占用检查、追踪页显示可用容量 |
| 物品管理 | 编辑、移动、复制和物品目录 | 按名称、ID、槽位搜索，筛选有物品或空槽位 |
| 人物关系 | 单人搜索与步进调整 | 点数直接输入、人物筛选、按当前结果批量预览补满 |
| 送礼状态 | 未解析 | 展示并重置已有的今日、本周送礼计数，支持单人和批量 |
| 配方与农场操作 | 已有解锁、地图和批量清理 | 继续使用现有实现 |

箱子/冰箱、社区中心和建筑编辑尚未覆盖；它们需要额外的结构与字段联动支持。

## 使用方式

1. 加载农场后进入“工具 → 背包物品”，选择容量，或用搜索和槽位筛选找到物品。缩容时若末尾有物品，先将它们移入保留的槽位。
2. 进入“工具 → 人物关系”，搜索角色或调整人物筛选，再点“补满好感”或“重置送礼次数”。预览列出本次角色和变化值，点“加入草稿”后才改变草稿。
3. 从“检查与保存”核对差异，可以逐项撤销，再统一保存。批量操作本身不会立即写回文件。

## 数据规则

- 容量编辑要求已有有效 `maxItems` 和标准背包。读取时补齐可用空格只用于草稿；其他编辑不会把这些空格无故写入存档。
- `inventorySlotFloor` 记录原始序列化长度，缩容保留原来的空槽；非空末尾槽位会阻止缩容。向未解锁槽位写入新物品在写回边界被拒绝。
- 撤销容量扩展会恢复新解锁槽位，保留原有可用槽位上的独立编辑。差异页明确说明这一依赖关系。
- 批量好感仅提高符合条件角色的点数：普通可恋爱角色 2,000、朋友/约会 2,500、配偶 3,500。更高点数保留；未知角色、未知关系、订婚与离婚不自动加入。
- 送礼仅重置已存在且能解析为非负整数的计数。日期、好感、状态与聊天标记不跟随改变；缺失或无效值保留原 XML。
- 修改一个人物不会重写其他人物；修改一个背包槽位保留其他槽位及容器内未知子节点。
- 新增字段参与差异生成、撤销和既有事务保存；容量只同步摘要中已有字段，送礼与好感仍只写主存档。

## 验证状态

- Xcode 16.4 原生 Debug 编译及模拟器测试通过：实际执行 79 项，79 项通过，0 项失败或跳过；包括新增 14 项存档回归和 3 项界面测试。
- Xcode 16.4 / iOS SDK 18.5 原生 Release 设备编译通过，生成 0.4.0（10）未签名 arm64 IPA。
- 6 项工程/版本/数据/资源静态检查通过，113 张位图校验通过。
- 已检查本次测试输出的 6 张编辑界面截图；背包搜索截图保留键盘输入态，筛选结果由 XCTest 元素断言验证。
- 测试使用 iPhone SE（第 3 代）/ iOS 26.2 模拟器和合成存档。真机安装、真实游戏读档/隔日往返，以及完整 iPad、动态字体和 VoiceOver 验证仍未执行。

原生构建、逐项测试结果、截图和源码一致性证据见 [BUILD10_VERIFICATION.md](validation/BUILD10_VERIFICATION.md)。可执行 `bash scripts/validate-on-macos.sh` 复测，或执行 `bash scripts/build-unsigned-ipa.sh` 构建设备包。
