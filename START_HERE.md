# 穗光琥珀存档匣：安装与使用

版本：0.7.0（构建 15）。适用于 iOS 17 及以上的 iPhone 和 iPad，面向《星露谷物语》1.6.x 主玩家存档。

## 安装

交付文件 `SheaflightAmberVault-v0.7.0-build15-unsigned.ipa` 是未签名安装包。请使用你已有的 iOS 签名工具和有效签名资料签名后安装。也可以在 Mac 上用 Xcode 16 或以上版本打开源码中的 `PelicanSaveEditor.xcodeproj`，选择自己的 Signing Team 后运行到设备。

源码和 IPA 均不包含个人证书或描述文件。模拟器测试、设备 Release 构建及 IPA 静态检查的详细结果见源码中的 `validation/BUILD15_VERIFICATION.md`；签名真机安装与真实游戏往返尚未验证。

## 导入并保存

1. 关闭游戏，首次试用时使用独立存档副本。
2. 在编辑器主页打开系统文件选择器，选择 `Stardew Valley` 外层目录或单个农场目录；也可同时选择主存档与 `SaveGameInfo` 两个配对文件。
3. 在“工具”中选择要修改的板块。各页修改先进入草稿，可切换板块后继续编辑。
4. 打开“检查与保存”，核对差异后保存。应用会执行自动备份和配对文件写入；原位写入不可用时，可复制导入并导出配对文件。
5. 在游戏中载入修改后的副本，检查修改值，再睡觉保存并重新载入，确认游戏保留了这些修改。

## 本轮新增入口

- **工具与装备**：选择存档中已有的工具、武器或鞋子，修改开放的属性后加入草稿。工具升级支持 0–4 级；取消关闭本次输入，恢复可回到读取时属性，检查页支持逐项撤销。
- **收藏与缺失清单**：查看博物馆、钓鱼、矿物、古物和出货记录，按名称或编号搜索。点击补给会向首个可用背包空位加入一件物品；博物馆捐赠等游戏操作仍需回游戏完成。
- **保存修复**：配方只写实际改变的记录；异常物品、重复身份或歧义坐标不开放相应编辑；写回前重新解析生成的存档并核对草稿意图。

收藏列表中的“无法确认”表示存档中相应记录缺失或异常，不等于已完成或未完成。装备编辑保留附魔、锻造和未知扩展字段，不提供完整锻造编辑。

## 交付文件

最终交付目录中包含 IPA、源码 ZIP、从构建 14 到构建 15 的补丁、文件摘要及验证记录。`BUILD15-DELIVERY.json` 记录源码与编译测试提交的对应关系，`SHA256SUMS.txt` 可用于核对文件完整性。完整修复范围见源码中的 `COMPLETE_REPAIR.md`；安装包体积原因见 `validation/BUILD15_SIZE_COMPARISON.md`。

交付目录的 `validation/native-logs/` 保留 iPhone 全量、iPhone 修复 UI 复验和两种 iPad 共四份原始测试日志，其 SHA-256 与逐项报告相互对应。原 iPhone 全量中的三项导航失败完整保留，最终覆盖由未变的 169 项和后续整组通过的 4 项组合核对。
