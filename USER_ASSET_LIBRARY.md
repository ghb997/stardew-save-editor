# 用户素材库接入记录

输入：`星露谷素材800+.zip`。应用只使用选中的图片，不包含原始压缩包，也不执行压缩包内容。

## 审计结果

- 完整压缩包 SHA-256：`4caed66f090c1ff18bf220f08718a73afe4da2b3e1564f6a5f88352cc5c8f79b`
- 文件总数：859，全部可解码为图片
- 格式：803 PNG、27 GIF、29 JPEG
- 动画图片：27
- 未发现绝对路径、`..` 路径穿越、符号链接、非图片文件或重复字节文件
- 最常见尺寸：48×48（469 张）、128×128（107 张）、64×64（47 张）

## 接入原则

当前软件已经通过 `springobjects`、`Objects_2`、`Craftables` 与 `tools` 图集覆盖标准物品、配方和工具。此次选择 58 张图片：27 张补齐动物、果树、NPC 与鱼塘实体预览，31 张作为板块入口、技能、进度、钱包、地图操作、备份和空状态的识别图。实体图可由存档类型、NPC 名称或对象 ID 对应；界面图只说明当前功能，不代表存档已经持有或解锁图中的物品。

动物和果树图片是物种预览。鸡图是幼年形态，果树图是成熟形态，界面不把它们描述为实际年龄或当前生长阶段。没有可靠匹配的颜色、形态或模组内容保持无预览。

机器可读的逐文件来源、尺寸与 SHA-256 位于 `GAME_ASSETS_MANIFEST.json`；素材代码映射位于 `PelicanSaveEditor/Views/Components/GameArtwork.swift`。工具页、追踪页和编辑器固定顶栏使用独立板块图，技能、财富、收藏与农场操作行也显示对应原图。

## 未直接接入的内容

- GIF 表情与场景动画：当前界面不需要动画解码，并且这些文件不能准确反映存档状态。
- 房屋、节日场景和大型装饰：多数不是农场建筑节点的一对一外观。
- 没有承担界面识别用途的标准物品、食物、矿物、武器和工具单图：现有游戏图集已经按 ID 精确覆盖。
- 虚空鸡、金鸡、恐龙和肤色色板：素材库没有可确认的对应图片。
- 宠物、马和野生动物：当前编辑模型不读取这些节点，暂不显示易产生误解的静态图。
