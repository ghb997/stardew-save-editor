# 构建 15 修复说明

源码候选版本 0.7.0（15），基于构建 14 的 `94caf12`，位于独立分支 `codex/complete-repair-build15`。此前交付的 IPA 和源代码目录保持原样。本轮工作针对存档写入问题及已确认的装备、收藏功能缺口；安装包大小不是功能完整性或保存可靠性的验收指标。

## 保存修复

| 问题 | 当前实现 |
| --- | --- |
| 解锁一个配方会重写无关配方的计数 | 仅修改发生变化的配方，保留其他计数的原始文本和扩展字段 |
| 缺少配方容器时，解锁操作可能没有写入 | 实际解锁时创建对应容器；支持标准 SerializableDictionary 包装 |
| 重复、异常或未知配方可以进入修改流程 | 识别为只读，批量解锁跳过这些记录 |
| 背包模板、只读物品或重复身份缺少统一校验 | 背包和容器共用写入规则；新物品来自内置模板，原物品保留未开放字段 |
| 只改数量也可能改动品质文本 | 数量和品质独立写入，保留未变字段如 `00` 的原始表示 |
| 标记为空但仍带数据的物品可能被覆盖 | 将该槽位视为只读异常记录；重复或空值背包容器不开放编辑 |
| 重复 NPC、动物编号或等价坐标可定位到多条记录 | 排除歧义目标；动物字典键与 myID 冲突时不开放编辑；坐标统一按数值判断 |
| XML 能解析不代表草稿已生效 | 后台生成目标数据后重新解析，核对编辑值；缺失字段造成的无效写入在覆盖原档前报错 |
| 组合操作改变 XML 索引后可能错误判定保存失败 | 装备、容器、机器按稳定遍历顺序与地点、类型核对；农场操作按坐标和状态核对 |

原有自动备份、双文件事务、来源哈希冲突检测和失败回滚继续使用。重新解析核对验证的是编辑器的序列化结果，不等于游戏已经成功载入或保留这些修改。

## 工具与装备

入口位于工具页。支持已有背包物品、标准箱子/冰箱中的物品，以及当前穿戴的鞋子。

| 类型 | 开放字段 |
| --- | --- |
| 斧头、镐、锄头、喷壶 | 0–4 级工具升级 |
| MeleeWeapon 武器 | 已存在的最低/最高伤害、速度、防御、精准、暴击概率/倍率、击退 |
| Boots 鞋子 | 已存在的防御、免疫 |

只有唯一、有效的现有数值字段会显示。武器最低伤害不能高于最高伤害；空值、非数字、无穷大和超范围输入不能加入草稿。弹窗使用局部副本，取消不会修改全局草稿；可恢复读取时属性，也可在检查页撤销某个字段。

不创建缺失装备、不替换工具类型，不编辑附魔、锻造、戒指组合、钓竿配件或未知 Mod 类型。字段范围是编辑器允许的输入范围，不是已通过真机游戏验证的兼容承诺。

## 收藏与缺失清单

博物馆按实际 `museumPieces` 捐赠记录核对内置 95 种原版候选品，独立于 `archaeologyFound` 发现记录。钓鱼和古物支持数组计数，矿物与出货读取相应计数。标准 ID 与 `(O)` 前缀 ID 会归一化。

每项分为“已有记录”“缺少记录”“无法确认”。容器缺失、nil、重复、畸形数据或冲突坐标不会伪装成正常的零进度。目录外记录保持原样，并提示其数量。

可搜索名称或编号、按状态筛选，也可补给一个目录物品到首个可用背包空位。补给只产生背包草稿，不直接标记捐赠、捕获、出货或发现。钓鱼和出货目录是当前内置目录的筛选结果，不宣称等于游戏完美度所需的全部项目。

## 验证范围与剩余工作

- 新增 31 项单元测试声明，涵盖原始数据保留、模板边界、畸形记录、组合操作、数组收藏记录与后台写回验证。
- 新增 4 条界面测试声明，涵盖工具升级检查、取消无副作用、非法武器输入及恢复、收藏补给。
- 已注册所有 Swift 源文件、单元测试及 UI 测试，统一版本为 0.7.0（15）。完整 iPhone 测试、两种 iPad 尺寸的新增流程及 Release IPA 构建已接入现有 GitHub Actions 触发范围。
- 本轮原生编译和上述测试尚未执行，登录恢复后必须运行并修复任何编译/测试失败，再生成 IPA。
- 真实游戏的加载、操作、睡觉保存及二次载入尚未验证。完整地形/人物渲染、垃圾桶/矿洞/夜间事件等高级预测、完整锻造编辑与全部多人/Mod 兼容仍属于未完成的扩展功能，不能宣称与参考应用全面等价。

本轮没有向参考 IPA 提取代码或资源加入项目，也没有通过填充素材人为增加软件大小。
