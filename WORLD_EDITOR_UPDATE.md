# 魔法地图、人物外观、房屋与房间增强

版本 0.5.0（构建 11）沿用本地解析、草稿差异、逐项撤销、备份和事务保存流程。新增操作先保存在草稿中。编辑页和地图的底部检查栏占用独立布局空间，最后一行可以完整滚动到检查栏上方；修复了小屏颜色页的恢复按钮被遮挡的问题。

## 魔法地图

“搜索与范围”支持名称、种子 ID 和坐标文本搜索；可以按实体类型、可操作或待处理状态筛选，再限定包含边界的矩形 X/Y 范围。无效范围不会回退到全图操作。浇水和清理按钮显示当前可新增的对象数，确认页保留本次对象清单。

干燥的存活作物可单株浇水，散落石块、杂草与树枝可单项清理。列表与坐标图共享草稿，可以定位对象、撤销操作。已被全图操作覆盖的对象会提示其来源；撤销相应全图操作后即可单独选择。大图支持平移、双指或滑杆缩放到 4 倍，并可复位。

浇水只写主存档 Farm 内有效作物的已有 state/netState 字段，支持 HoeDirt 和省略类型标记的标准 TerrainFeature 结构。已浇水、枯萎、缺少状态、空值或显式未知类型不参加；不会生成缺失字段。单项操作的坐标键在写入时再次对照原始对象校验。

## 人物外观

保留性别、发型、肤色、饰品选择与原始/草稿对照，增加编号直接输入和当前类别恢复。颜色页面可切换发色、眼睛与裤子颜色，提供系统选色器、六位 RGB 色值、常用色、原始对照和恢复。

仅开放原存档中可解析的颜色字段，支持 RGBA 通道和 XNA PackedValue；两者同时存在时必须一致。写入保留透明度、未知子节点及属性；SaveGameInfo 仅同步已有且有效的颜色字段。不修补不明或缺失颜色，不承诺色块等同游戏光照下的完整人物渲染。

## 房屋与房间

房间按名称或存档键搜索，墙纸与地板可切换，显示原始和当前草稿的贴图。标准样式库改为可搜索的纵向网格；墙纸提供 0–111，地板提供 0–55，扩展编号可直接输入。房间列表表达已有房间和表面，不推测家具或实际建筑坐标。

“套用到当前筛选房间”先显示将变更的同类表面、房间名与前后编号，允许取消个别房间。支持恢复当前房间的墙纸与地板，或撤销全部房屋修改。未修改的表面保留原始 XML 数值写法；空值、未知结构、重复房间键保持只读。

## 验证范围

新增 18 项单元回归覆盖颜色序列化、写入边界、房间范围/恢复和地图选择/写入；新增 3 项 XCUITest 覆盖颜色预设与色值、房间样式与批量预览、地图搜索与单株浇水。2026-09-10 已完成设备 Release 编译与模拟器 Debug 验证，实际执行 100 项测试、全部通过，并检查七张新流程原生截图。测试使用合成数据，详细证据和范围见 [构建 11 验证记录](validation/BUILD11_VERIFICATION.md)。

真机签名安装、真实游戏存档往返及完整 iPad/辅助功能检查需要单独验收。此版本未加入家具拖拽、建筑移动、室内物件或联机其他玩家编辑。

## 操作示例

1. 在“工具 → 魔法地图 → 搜索与范围”输入名称、种子 ID 或坐标，例如 X 14；用“限定坐标范围”输入起点和终点。点击“预览浇水”查看对象清单，再加入草稿。切到“待处理”可定位该作物或撤销。
2. 在“工具 → 人物外观”打开颜色页，选择发色、眼睛或裤子。色值旁的清空按钮可清除输入文本，当前颜色会保留；输入六位色值（例如 #123456）并点击“应用色值”后才应用。原始与当前色块可直接比较；“恢复原始”只撤销所选部位的颜色。
3. 在“工具 → 房屋与房间”搜索房间，选择墙纸或地板后打开样式库，搜索编号并点选样式。返回后可将它套用到当前筛选房间，预览页允许逐个取消勾选。恢复当前房间同时恢复它的墙纸和地板；恢复全部房屋修改也会撤销农舍等级改动。
4. 完成编辑后进入“检查与保存”，核对实际变化，再保存。放弃某项编辑时可在检查页逐项撤销。
